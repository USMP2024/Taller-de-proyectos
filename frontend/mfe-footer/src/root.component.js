import React from 'react';
import Sidebar from './components/Header'; // Asegúrate de que la ruta de importación sea correcta

function Root(props) {
  return (
      <Sidebar />
  );
}

export default Root;
